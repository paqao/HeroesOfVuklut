public class GameManager {
  private readonly Game _game;
  private readonly Settings _settings;
  
  public GameManager(Game game, Settings settings){
    _game = game;
    _settings = settings;
  }
  
}
